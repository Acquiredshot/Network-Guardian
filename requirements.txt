pyyaml>=6.0,<7
psutil>=5.9,<7
watchdog>=4.0,<7
reportlab>=4.0,<5
setuptools>=68.0,<76
wheel

# desktop UI (optional; only needed for --desktop)
PyQt5>=5.15,<6

# dev / test
pytest>=8.0
pytest-asyncio>=0.23
